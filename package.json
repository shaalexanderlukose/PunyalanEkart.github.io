{
  "name": "your-project-name",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.17.1",
    "firebase-admin": "^10.0.0" // Firebase Admin SDK
    // Other dependencies as needed
  },
  "scripts": {
    "start": "node server/server.js" // Start script for the server
  }
}
