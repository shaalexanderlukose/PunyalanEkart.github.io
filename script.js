// DOMContentLoaded event ensures the script runs after the HTML content is loaded
document.addEventListener('DOMContentLoaded', () => {
  // Your code here for handling user interactions or other functionalities
  // Example: Adding an event listener to a button click
  const myButton = document.getElementById('myButton'); // Replace 'myButton' with your button ID
  myButton.addEventListener('click', () => {
    // Perform actions when the button is clicked
    // Example: Display an alert
    alert('Button clicked!');
  });

  // Other functions or event listeners can be added here
});
